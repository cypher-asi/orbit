services:
  db:
    image: postgres:16
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: orbit
    volumes:
      - orbit_pgdata:/var/lib/postgresql/data

volumes:
  orbit_pgdata:
